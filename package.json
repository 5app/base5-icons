{
  "name": "base5-icons",
  "version": "0.0.5",
  "description": "Repository for 5app's React icons",
  "main": "index.js",
  "scripts": {
    "babel": "babel src/jsx -d dist",
    "build": "svgr --icon --replace-attr-value '#000=currentColor' --ext 'jsx' --use-tabs --tab-width 4 -d src/jsx src/svg",
    "postbuild": "npm run babel",
    "copypackage": "cp -rf package.json dist",
    "predist": "npm run copypackage",
    "dist": "cd dist && npm publish"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/5app/base5-icons.git"
  },
  "keywords": [
    "icons",
    "5app"
  ],
  "author": "Dionysos Dajka",
  "license": "MIT",
  "dependencies": {
    "react": "^16.3.1"
  },
  "devDependencies": {
    "svgr": "^1.9.1",
    "babel-cli": "^6.26.0",
    "babel-plugin-transform-react-jsx": "^6.24.1",
    "babel-preset-env": "^1.6.1"
  }
}
