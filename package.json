{
  "name": "base5-icons",
  "version": "1.0.3",
  "description": "Repository for 5app's React icons",
  "main": "icons/index.js",
  "scripts": {
    "build": "svgr --icon --replace-attr-value '#000=currentColor' --ext 'jsx' --use-tabs --tab-width 4 -d icons icons-src",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "icons",
    "5app"
  ],
  "author": "Dionysos Dajka",
  "license": "MIT",
  "dependencies": {
    "react": "^16.3.1",
    "svgr": "^1.9.1"
  }
}
